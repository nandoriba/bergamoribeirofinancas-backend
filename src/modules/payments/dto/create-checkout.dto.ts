/**
 * Intentionally empty: checkout identity, product, amount, method and redirect
 * URLs are all derived server-side. ValidationPipe rejects any supplied field.
 */
export class CreateCheckoutDto {}
